@model IEnumerable<Core.Domain.Entities.Client>
@{
    ViewData["Title"] = "Clients";
}

<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="h4 mb-0">Clients</h2>
        <a asp-action="Create" class="btn btn-primary"><img src="~/images/edit-add.svg" alt="Add" class="action-icon me-1"/> New Client</a>
    </div>

    @if (Model == null || !Model.Any())
    {
        <div class="alert alert-info">No clients found.</div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Phone</th>
                        <th>Job</th>
                        <th>ID Card</th>
                        <th>Passport</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var c in Model)
                    {
                        <tr>
                            <td>@c.Id</td>
                            <td>@($"{c.FirstName} {c.LastName}")</td>
                            <td>@c.Email</td>
                            <td>@(string.IsNullOrEmpty(c.PhoneNumber) ? "-" : c.PhoneNumber)</td>
                            <td>@c.Job</td>
                            <td>@c.IdCard_Number</td>
                            <td>@c.Passport_Number</td>
                            <td>
                                <a asp-action="Details" asp-controller="Clients" asp-route-id="@c.Id" class="btn btn-sm btn-outline-secondary me-1">
                                    <img src="~/images/informations.png" alt="View" class="action-icon table-action-icon" />
                                </a>
                                <a asp-action="Edit" asp-controller="Clients" asp-route-id="@c.Id" class="btn btn-sm btn-outline-primary me-1">
                                    <img src="~/images/editer.png" alt="Edit" class="action-icon table-action-icon" />
                                </a>
                                <a asp-action="Delete" asp-controller="Clients" asp-route-id="@c.Id" class="btn btn-sm btn-outline-danger me-1">
                                    <img src="~/images/delete.png" alt="Delete" class="action-icon table-action-icon" />
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>

